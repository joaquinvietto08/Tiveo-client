import { StyleSheet } from "react-native";
import { colors } from "../../../../../styles/globalStyles";

export const styles = StyleSheet.create({
  WR__advanceBottom__container: {
    marginTop: "auto",
    paddingBottom: 60,
    paddingTop: 40
  },
});
